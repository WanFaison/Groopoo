export type ListeModel = {
    id: number,
    libelle: string,
    critere: string,
    annee: number,
    ecole: string,
    date: string,
    count: number,
    isComplet: boolean,
    isImport?: boolean
}
